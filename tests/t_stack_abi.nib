fn swap_pair(a: u16 in stack, b: u16 in stack) -> u16 in stack, u16 in stack {
    return b, a;
}

fn caller() -> u16 {
    u16 lo;
    u16 hi;
    lo, hi := swap_pair(0x1111, 0x2222);
    return lo + hi;
}
