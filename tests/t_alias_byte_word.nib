// Test: byte vreg must not be clobbered by word vreg using aliased register.

fn accumulate(base: u16, count: u8) -> u8 {
    u8 total = 0;
    u8 i = 0;
    while (i < count) {
        u16 addr = base + 100;
        total := total + i;
        i := i + 1;
    }
    return total;
}
